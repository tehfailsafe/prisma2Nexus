# Migration `20190810102908-update`

This migration has been generated by Mike Johnson at 8/10/2019, 10:29:08 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "lift"."Post";
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190810102140-init..20190810102908-update
--- datamodel.dml
+++ datamodel.dml
@@ -15,16 +15,5 @@
 model User {
   id    String  @default(cuid()) @id @unique
   email String  @unique
   name  String?
-  posts Post[]
-}
-
-model Post {
-  id        String   @default(cuid()) @id @unique
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  published Boolean
-  title     String
-  content   String?
-  author    User?
-}
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190810102908-update)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190810102908-update'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
