# Migration `20190810103900-update`

This migration has been generated by Mike Johnson at 8/10/2019, 10:39:00 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Post"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"published" BOOLEAN NOT NULL DEFAULT false ,"title" TEXT NOT NULL DEFAULT '' ,"content" TEXT   ,"author" TEXT   REFERENCES "User"(id) ON DELETE SET NULL,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "lift"."Post.id._UNIQUE" ON "Post"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190810102908-update..20190810103900-update
--- datamodel.dml
+++ datamodel.dml
@@ -15,5 +15,16 @@
 model User {
   id    String  @default(cuid()) @id @unique
   email String  @unique
   name  String?
+  posts Post[]
+}
+
+model Post {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  published Boolean
+  title     String
+  content   String?
+  author    User?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190810103900-update)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190810103900-update'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
